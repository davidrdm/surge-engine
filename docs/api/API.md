# Surge I&W — API reference

<!-- Generated by scripts/build_api_contract.py. Do not edit. -->

Version `0.6.0`. OpenAPI `3.1.0`.

Tactical indications and warning from four independent open sources.

Alerts correlate four independent sources — social chatter, military and charter
flights, short-term-rental availability, and rental-car availability — into a
deterministic confidence score. The score is computed in Python from a weighted
model whose working is recorded per alert; a language model writes the summary
sentence and cannot change the number.

Read `/v1/alerts/{id}/evidence` before acting on a score. It resolves every
contributing signal back to the raw payload and the queue row that fetched it,
and reports the band rule that fired and any collection gap that capped it.

**A failure is never an absence of threat.** A source that could not be
collected lowers `data_completeness`, caps the band below HIGH, and is named in
the alert's caveat. An alert on a PARTIAL iteration is a real finding made from
incomplete evidence, not a quieter one.

## Authentication

Every route except `GET /v1/healthz` requires a bearer token, read from the environment variable named by `api.token_env` (`SURGE_API_TOKEN` by default).

```
Authorization: Bearer <SURGE_API_TOKEN>
```

An unset token makes the API return **503** rather than serving unauthenticated: a check that switches itself off when unconfigured is worse than none, because it looks like one.

## Endpoints

| Method | Path | Purpose |
|---|---|---|
| `POST` | [`/v1/sessions`](#post-v1sessions) | Initialise a session with its key locations. |
| `GET` | [`/v1/sessions/{session_id}`](#get-v1sessionssession_id) | The session as configured, with its resolved geography. |
| `POST` | [`/v1/sessions/{session_id}/cities`](#post-v1sessionssession_idcities) | Add cities between iterations, never during one. |
| `GET` | [`/v1/iterations/{iteration_id}`](#get-v1iterationsiteration_id) | The poll target for the 202. |
| `POST` | [`/v1/iterations/{iteration_id}/cancel`](#post-v1iterationsiteration_idcancel) | Ask a running iteration to stop early. |
| `POST` | [`/v1/iterations/{iteration_id}/retry-triage`](#post-v1iterationsiteration_idretry-triage) | Re-judge the posts this iteration's model calls never answered (8.8). |
| `GET` | [`/v1/sessions/{session_id}/iterations`](#get-v1sessionssession_iditerations) | This session's iterations, most recent first. |
| `POST` | [`/v1/sessions/{session_id}/iterations`](#post-v1sessionssession_iditerations) | Trigger one iteration. The only thing that starts collection. |
| `GET` | [`/v1/alerts/{alert_id}/evidence`](#get-v1alertsalert_idevidence) | Every contributing signal back to the raw payload and the queue row. |
| `POST` | [`/v1/alerts/{alert_id}/review`](#post-v1alertsalert_idreview) | Record a human decision about distributing this alert. |
| `GET` | [`/v1/sessions/{session_id}/alerts`](#get-v1sessionssession_idalerts) | The required output: alerts with their evidence, most severe first. |
| `GET` | [`/v1/sessions/{session_id}/queue`](#get-v1sessionssession_idqueue) | Queue state and every refusal, by outcome. |
| `GET` | [`/v1/capabilities`](#get-v1capabilities) | What this deployment can and cannot collect, stated up front. |
| `GET` | [`/v1/healthz`](#get-v1healthz) | Liveness. Unauthenticated by design; vendor probing is not. |
| `POST` | [`/v1/iterations/{iteration_id}/discard-last-stage`](#post-v1iterationsiteration_iddiscard-last-stage) *(debug)* | Remove the last stage's output and point the iteration back at it. |
| `GET` | [`/v1/iterations/{iteration_id}/stages`](#get-v1iterationsiteration_idstages) *(debug)* | What happened in every stage, including the ones that never ran. |
| `GET` | [`/v1/iterations/{iteration_id}/stages/{stage}`](#get-v1iterationsiteration_idstagesstage) *(debug)* | One stage in detail: what it wrote, who ran, what it spent, its log. |
| `POST` | [`/v1/iterations/{iteration_id}/step`](#post-v1iterationsiteration_idstep) *(debug)* | Run exactly one stage and return what it did. |
| `GET` | [`/v1/correlations/{correlation_id}/evidence`](#get-v1correlationscorrelation_idevidence) | The same drill-down, for a correlation that produced no alert (8.7b). |
| `POST` | [`/v1/iterations/{iteration_id}/abandon`](#post-v1iterationsiteration_idabandon) | Close an interrupted iteration, counting the lost collection honestly. |
| `GET` | [`/v1/iterations/{iteration_id}/correlations`](#get-v1iterationsiteration_idcorrelations) | Every correlation this iteration scored, alerting or not. |
| `GET` | [`/v1/iterations/{iteration_id}/recovery-plan`](#get-v1iterationsiteration_idrecovery-plan) | What a resume would re-collect, and what it would leave alone. |
| `POST` | [`/v1/iterations/{iteration_id}/resume`](#post-v1iterationsiteration_idresume) | Continue an interrupted iteration from where it stopped. |
| `GET` | [`/v1/recovery`](#get-v1recovery) | What this process found when it started, and what still needs a decision. |

The 4 paths marked *(debug)* are mounted only when `api.debug_endpoints` is true. `openapi-operational.json` is the surface without them — generate a client from that one for a deployment, or it will call 4 paths that return 404.

## Errors

Non-2xx responses carry `{"detail": "..."}` — the `ErrorOut` schema — with one exception noted below.

| Status | Meaning |
|---|---|
| `401` | Missing or invalid bearer token. |
| `404` | No such session, iteration or alert. |
| `409` | Refused because of current state: an iteration is already running for this session, the iteration is not at the stage you asked for, or a paid stage needs an explicit confirm. |
| `422` | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**`422` has two shapes.** A request that fails schema validation returns FastAPI's `HTTPValidationError` — a `detail` *array* naming the offending field. A request that is well-formed but cannot be acted on (an unknown stage, a malformed timestamp, a session with no cities) returns `ErrorOut`, whose `detail` is a string. Handle both: the responses table on each endpoint below says which one that endpoint declares, and both are reachable wherever path or query parameters exist.

---

## Reference

### `POST /v1/sessions`

Initialise a session with its key locations.

Airport and pickup mappings are resolved here rather than at tip time, so a
city the geo table cannot place is reported as a warning before anything is
collected. Discovering it mid-iteration would produce a SKIPPED_NO_MAPPING
query, which is correct but arrives hours later and reads as a data gap
rather than a setup mistake.

**`input_set` loads the geography from a file** (8.7c) instead of inlining
it — the NAME of a file in `inputs.dir`, never a path. The file is resolved
all-or-nothing: a city the geo table cannot place is refused by name with a
422, because a session quietly missing a jurisdiction would report a true
absence of evidence about a place nobody looked at. The response echoes the
resolved geography either way, so an operator sees what they created rather
than trusting that a file still says what they remember.

**Request body** — `SessionIn`

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `201` | SessionOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `422` | ErrorOut | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/01-create-session.json`](examples/01-create-session.json)


### `GET /v1/sessions/{session_id}`

The session as configured, with its resolved geography.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `session_id` | path | yes | integer |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | SessionOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/02-unauthenticated.json`](examples/02-unauthenticated.json)


### `POST /v1/sessions/{session_id}/cities`

Add cities between iterations, never during one.

Refused while an iteration is in flight because stage 1 has already fixed
the city set it will collect against; a city added mid-run would be seeded
by the next iteration anyway, but its absence from this one's correlation
would be invisible.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `session_id` | path | yes | integer |  |

**Request body** — `CitiesIn`

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `201` | SessionOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `409` | ErrorOut | Refused because of current state: an iteration is already running for this session, the iteration is not at the stage you asked for, or a paid stage needs an explicit confirm. |
| `422` | ErrorOut | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/18-add-cities.json`](examples/18-add-cities.json)


### `GET /v1/iterations/{iteration_id}`

The poll target for the 202.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | IterationOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/05-poll-iteration.json`](examples/05-poll-iteration.json)


### `POST /v1/iterations/{iteration_id}/cancel`

Ask a running iteration to stop early.

**Cooperative, not a kill.** The request is recorded and honoured at the
next stage boundary; the iteration then runs CORRELATING and ALERTING and
closes PARTIAL. A hard stop would spend money on collection and throw away
the evidence it bought, and would produce no alert at all for a city whose
coverage may be nearly complete — a real cluster reading as silence, which
is the one outcome this system exists to prevent.

The skipped stages are recorded as degradations and the uncollected work
flows into `data_completeness`, so the band is capped and the caveat names
the gap. Cancelling cannot launder a partial run into full confidence.

409 if the iteration has already finished — there is nothing to stop, and
reporting success would imply the run was shortened when it was not.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |

**Request body** — `CancelIn` (optional)

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | CancelOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `409` | ErrorOut | Refused because of current state: an iteration is already running for this session, the iteration is not at the stage you asked for, or a paid stage needs an explicit confirm. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/34-cancel.json`](examples/34-cancel.json)


### `POST /v1/iterations/{iteration_id}/retry-triage`

Re-judge the posts this iteration's model calls never answered (8.8).

A batch that overruns `llm.max_tokens` records **every** post in it as
`MODEL_ERROR`: the evidence was collected and paid for, only the judgement
is missing. This recovers those judgements and lets them tip collection.

**It creates a NEW iteration**, a child of this one, and never edits the
parent. The parent stays as it was — partial, degraded, its gap named — and
the child carries `retry_of_iteration_id`. Two records in the order they
happened rather than one rewritten after the fact.

The child inherits the parent's `anchor_at`, because the correlation window
is measured from it and a fresh anchor would slide the window off the
evidence the retry exists to complete. It runs TRIAGING through ALERTING:
SEEDING and COLLECTING_SOCIAL are inherited rather than re-run, and
SCHEDULING is skipped because the parent already queued follow-ons for this
same evidence. Correlation reads signals across the session by observation
time, so the child scores the union of both runs' evidence without anything
being copied.

**Only `UNDECIDED`, `INVALID_OUTPUT` and `MODEL_ERROR` are retried.**
ACCEPTED and REJECTED are completed judgements and a rejection is a
conclusion, not a failure. A post dropped for being older than
`triage.max_post_age_hours` has no decision row at all, so it cannot be
reached — that requirement holds by construction rather than by a second
filter that could drift from the first.

**Send an `Idempotency-Key`.** This endpoint spends model tokens and tips
paid collection, and it is *more* dangerous to retry blindly than the
trigger is: the parent's `MODEL_ERROR` rows are deliberately never edited,
so the candidate set is unchanged by a successful retry and a repeated call
would create another child and spend again, indefinitely. With a key the
retry replays the first response and starts nothing.

409 if the parent has not closed or the session has an iteration open;
422 if there is nothing to retry.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |
| `wait` | query | no | boolean | Run synchronously up to api.sync_timeout_s |
| `Idempotency-Key` | header | no | string | Opt-in replay protection. Repeating a POST with the same key returns the first response instead of starting a second retry. |

**Request body** — `RetryTriageIn` (optional)

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | IterationAccepted | Finished within ?wait=true's timeout. |
| `202` | IterationAccepted | Accepted; poll `poll_url` for progress. |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `409` | ErrorOut | Refused because of current state: an iteration is already running for this session, the iteration is not at the stage you asked for, or a paid stage needs an explicit confirm. |
| `422` | ErrorOut | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/40-retry-triage.json`](examples/40-retry-triage.json)


### `GET /v1/sessions/{session_id}/iterations`

This session's iterations, most recent first.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `session_id` | path | yes | integer |  |
| `limit` | query | no | integer |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | IterationOut[] | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |


### `POST /v1/sessions/{session_id}/iterations`

Trigger one iteration. The only thing that starts collection.

Returns 202 and a poll URL; the run continues on a worker. `?wait=true`
blocks up to the configured timeout for CLI use and still returns 202 if the
run outlives it — the iteration is not abandoned, because one that has
already paid for collection should finish and record what it bought.

**Send an `Idempotency-Key`.** This endpoint spends money. A client that
loses the response to a network timeout otherwise has no safe move: retry
and it may buy a second full collection pass, don't and there may have been
no run at all. With a key the retry replays the first response and starts
nothing, and the reply carries `Idempotent-Replay: true` so a caller can
tell. Reusing a key for a *different* body is refused with 422 rather than
silently answered with the old result.

A 409 here is retryable and carries `Retry-After`.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `session_id` | path | yes | integer |  |
| `wait` | query | no | boolean | Run synchronously up to api.sync_timeout_s |
| `Idempotency-Key` | header | no | string | Opt-in replay protection. Repeating a POST with the same key returns the first response instead of starting a second run. |

**Request body** — `IterationIn` (optional)

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | IterationAccepted | Finished within ?wait=true's timeout. |
| `202` | IterationAccepted | Accepted; poll `poll_url` for progress. |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `409` | ErrorOut | Refused because of current state: an iteration is already running for this session, the iteration is not at the stage you asked for, or a paid stage needs an explicit confirm. |
| `422` | ErrorOut | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/04-trigger-iteration.json`](examples/04-trigger-iteration.json)


### `GET /v1/alerts/{alert_id}/evidence`

Every contributing signal back to the raw payload and the queue row.

This is what makes a score arguable rather than merely reported. The
arithmetic is included — per-family weight × quality, the band rule that
fired, and the completeness that capped it — because whoever is asked to
act on a number is entitled to see how it was reached.

A signal whose raw payload has passed its retention deadline shows
`raw: null` with the query row intact. FR24's licence requires deletion
after 30 days; the analytical record deliberately outlives the licensed data.

**The evidence surface is the normalised record, not the vendor payload**
(8.2). By default `raw.payload` and the query parameters are withheld and
described rather than returned: what may be redistributed from a provider
response is a rights question per provider (8.3), not something this
endpoint should assume, and the parameters carry the search lexicon and
facility coordinates. What is always returned is everything needed to argue
with the conclusion — the normalised signal, its provenance and receipt, the
per-family arithmetic, and enough about the payload (provider, timestamps,
retention deadline, content hash) to demand it through another channel.
Set `api.expose_raw_payloads: true` to include them.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `alert_id` | path | yes | integer |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | EvidenceOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/08-evidence.json`](examples/08-evidence.json)


### `POST /v1/alerts/{alert_id}/review`

Record a human decision about distributing this alert.

Distribution only. The score, the band, the evidence and the receipts are
untouched — a reviewer governs what leaves the system, not what it
concluded. That separation is deliberate: an alert withheld for being
operationally unhelpful must remain in the record at its computed
confidence, or the audit trail becomes a record of what was published
rather than of what was found.

Setting UNREVIEWED again clears the reviewer and note rather than leaving
someone's name attached to a state they no longer hold.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `alert_id` | path | yes | integer |  |

**Request body** — `ReviewIn`

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | ReviewOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | ErrorOut | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/35-review-alert.json`](examples/35-review-alert.json)


### `GET /v1/sessions/{session_id}/alerts`

The required output: alerts with their evidence, most severe first.

`format=tuple` returns the four evidence groups as a positional array in the
order the requirement names them, for clients that want the literal tuple
shape rather than keys they would have to trust.

**Unfiltered means unfiltered.** Every alert is returned regardless of
review state, because an operator cannot review what the API hides. A
client that distributes onward should pass `review_state=RELEASED`;
one that shows an analyst a queue should pass `UNREVIEWED`. Defaulting to
RELEASED here would have made an unreviewed alert invisible rather than
unreviewed — the same failure as reporting a coverage gap as quiet.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `session_id` | path | yes | integer |  |
| `since` | query | no | string | ISO-8601 lower bound |
| `min_confidence` | query | no | string | LOW \| MEDIUM \| HIGH — inclusive floor |
| `city` | query | no | string |  |
| `track` | query | no | string |  |
| `iteration_id` | query | no | integer |  |
| `review_state` | query | no | string | UNREVIEWED \| RELEASED \| WITHHELD. A consumer distributing onward should ask for RELEASED. |
| `format` | query | no | string |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | AlertOut[] | AlertTupleOut[] | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | ErrorOut | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/06-alerts.json`](examples/06-alerts.json)


### `GET /v1/sessions/{session_id}/queue`

Queue state and every refusal, by outcome.

The refusals are the point. A query that was deduped, cooled down, capped or
priced out is a decision the system made, and an operator wondering why a
city was not searched needs to see it rather than infer it from silence.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `session_id` | path | yes | integer |  |
| `iteration_id` | query | no | integer |  |
| `limit` | query | no | integer |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | QueueOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/09-queue.json`](examples/09-queue.json)


### `GET /v1/capabilities`

What this deployment can and cannot collect, stated up front.

This endpoint exists because "no alerts for this county" and "this county
was never collectable" are identical on the wire, and the second silently
reads as reassurance. An unsupported jurisdiction is now reported as
unsupported.

With `?city=` it answers per jurisdiction: whether the name resolves, which
airports and pickup point it maps to, and which source families would
therefore produce nothing. Cheap and offline — it consults the geo tables
and the configuration, and calls no vendor.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `city` | query | no | string[] | Repeatable. Ask whether a jurisdiction is collectable BEFORE creating a session for it. |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | CapabilitiesOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/28-capabilities.json`](examples/28-capabilities.json)


### `GET /v1/healthz`

Liveness. Unauthenticated by design; vendor probing is not.

`deep=true` calls every connector's health check, which costs rate-limit
budget at four providers. Leaving that reachable without a token would make
an unauthenticated port a free way to exhaust the collection capacity this
system depends on, so the deep check requires the same bearer token as
everything else.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `deep` | query | no | boolean | Also probe the four vendors; requires authentication |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | HealthOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `422` | HTTPValidationError | Validation Error |

**Example** — [`examples/19-healthz.json`](examples/19-healthz.json)


### `POST /v1/iterations/{iteration_id}/discard-last-stage`

> Mounted only when `api.debug_endpoints` is true.

Remove the last stage's output and point the iteration back at it.

Only the last stage, and one at a time — discarding TRIAGING while TIPPING's
queries still reference its signals would break the guarantee that every
query traces to the post that caused it. Repeated calls walk backwards, so
any point is still reachable.

`api_calls` and `agent_log` are never deleted. The money is a fact about the
world rather than an output of the stage, and the audit trail has to record
the one destructive operation in the system rather than be erased by it.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |

**Request body** — `DiscardIn` (optional)

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | DiscardOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `409` | ErrorOut | Refused because of current state: an iteration is already running for this session, the iteration is not at the stage you asked for, or a paid stage needs an explicit confirm. |
| `422` | ErrorOut | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/15-discard-last-stage.json`](examples/15-discard-last-stage.json)


### `GET /v1/iterations/{iteration_id}/stages`

> Mounted only when `api.debug_endpoints` is true.

What happened in every stage, including the ones that never ran.

A stage the iteration never reached is as much a part of what happened as
one that did, so all eight are always present with an explicit status.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | StagesOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/13-stages.json`](examples/13-stages.json)


### `GET /v1/iterations/{iteration_id}/stages/{stage}`

> Mounted only when `api.debug_endpoints` is true.

One stage in detail: what it wrote, who ran, what it spent, its log.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |
| `stage` | path | yes | string |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | StageOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | ErrorOut | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/14-stage.json`](examples/14-stage.json)


### `POST /v1/iterations/{iteration_id}/step`

> Mounted only when `api.debug_endpoints` is true.

Run exactly one stage and return what it did.

The step response carries the stage's own report, so stepping and verifying
are one round trip rather than two — when a stage does something surprising
you want the counts in front of you, not a second URL to visit.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |

**Request body** — `StepIn` (optional)

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | StepOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `409` | ErrorOut | Refused because of current state: an iteration is already running for this session, the iteration is not at the stage you asked for, or a paid stage needs an explicit confirm. |
| `422` | ErrorOut | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/11-step.json`](examples/11-step.json)


### `GET /v1/correlations/{correlation_id}/evidence`

The same drill-down, for a correlation that produced no alert (8.7b).

Identical shape to `GET /v1/alerts/{id}/evidence` — that endpoint now
resolves its correlation and delegates here — with `alert_id`, `summary`,
`caveat` and `receipt` null when no alert was written, and
`alert_decision` saying why.

**This is the calibration surface.** A correlation below
`correlation.alert_min_score` is a near miss, and the near misses are what
the interim floors in `correlation` and `sensitivity` are meant to be set
from. Until now they were reachable only by opening the database, which also
meant no front end could ever show one.

Operational, not debug: an analyst tuning floors on a deployment serving
an operations team should not have to mount the endpoint that deletes
analytical records in order to do it. Read-only over rows that already
exist.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `correlation_id` | path | yes | integer |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | EvidenceOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/39-correlation-evidence.json`](examples/39-correlation-evidence.json)


### `POST /v1/iterations/{iteration_id}/abandon`

Close an interrupted iteration, counting the lost collection honestly.

The stranded queries become `INTERRUPTED`, which is in
`UNRELIABLE_QUERY_STATUSES` — so they lower `data_completeness`, cap the
band below HIGH, and get named in the alert caveat, with no change to the
scoring code.

Then it correlates and alerts on what *was* collected. Skipping that would
close the iteration with no alert at all for a city whose evidence may be
nearly complete, which is a real cluster reading as silence. It never
schedules follow-ons: work queued by an iteration nobody finished would
arrive as a surprise next run.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |

**Request body** — `AbandonIn`

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | AbandonOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `409` | ErrorOut | Refused because of current state: an iteration is already running for this session, the iteration is not at the stage you asked for, or a paid stage needs an explicit confirm. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/27-abandon.json`](examples/27-abandon.json)


### `GET /v1/iterations/{iteration_id}/correlations`

Every correlation this iteration scored, alerting or not.

One row per city and actor track that produced any evidence at all. The
ones that became alerts are reachable through `/v1/sessions/{id}/alerts`;
the ones that did not were, before 8.7(b), reachable nowhere.

`alert_decision` says what ALERTING concluded about each and why.
`evidence_url` drills into any of them.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | CorrelationsOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/08b-correlations.json`](examples/08b-correlations.json)


### `GET /v1/iterations/{iteration_id}/recovery-plan`

What a resume would re-collect, and what it would leave alone.

Read-only, and 409 rather than 200 when the iteration is not interrupted —
a client that got a 200 here might act on it.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | RecoveryPlanOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `409` | ErrorOut | Refused because of current state: an iteration is already running for this session, the iteration is not at the stage you asked for, or a paid stage needs an explicit confirm. |
| `422` | HTTPValidationError | Validation Error |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/24-recovery-plan.json`](examples/24-recovery-plan.json)


### `POST /v1/iterations/{iteration_id}/resume`

Continue an interrupted iteration from where it stopped.

The interrupted stage is re-run rather than skipped — it was incomplete —
and that is safe because every agent is re-entrant: collection claims only
PENDING rows, triage skips URLs it already ruled on, alerting skips
correlations that already have an alert, and correlation upserts.

Refuses without `confirm_respend` when the plan would collect again. FR24
bills per record returned and one historical query was measured at 60
credits, so this is a purchase and the caller has to say so.

**Parameters**

| Name | In | Required | Type | Description |
|---|---|---|---|---|
| `iteration_id` | path | yes | integer |  |
| `wait` | query | no | boolean |  |

**Request body** — `ResumeIn` (optional)

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | IterationAccepted | Finished within ?wait=true's timeout. |
| `202` | IterationAccepted | Accepted; poll `poll_url`. |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `404` | ErrorOut | No such session, iteration or alert. |
| `409` | ErrorOut | Refused because of current state: an iteration is already running for this session, the iteration is not at the stage you asked for, or a paid stage needs an explicit confirm. |
| `422` | ErrorOut | The request was understood but cannot be acted on — an unknown stage or band, a malformed timestamp, or a session with no cities. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/25-resume-needs-confirm.json`](examples/25-resume-needs-confirm.json)


### `GET /v1/recovery`

What this process found when it started, and what still needs a decision.

Every interrupted iteration is listed with the stage it died in and the two
URLs that can close it. There is no third option and no force-unstick: both
exits write a record.

**Responses**

| Status | Body | Meaning |
|---|---|---|
| `200` | RecoveryOut | Successful Response |
| `401` | ErrorOut | Missing or invalid bearer token. |
| `503` | ErrorOut | SURGE_API_TOKEN is not configured; the API refuses to serve without authentication rather than serving without it. |

**Example** — [`examples/22-recovery.json`](examples/22-recovery.json)


---

## Schemas

### `AbandonIn`

| Field | Type | Required | Description |
|---|---|---|---|
| `reason` | string | yes |  |
| `confirm` | boolean | no |  |
| `finalise` | boolean | no |  |

### `AbandonOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `iteration_id` | integer | yes |  |
| `outcome` | string | no |  |
| `queries_marked_interrupted` | integer | no |  |
| `coverage_gaps` | object | no | Source types now counted as missing collection, by count. These lower data_completeness and cap the band. |
| `correlations_written` | integer | no |  |
| `alerts_written` | integer | no |  |
| `scheduling_skipped` | boolean | no | Abandon never schedules follow-ons: work queued by an iteration nobody finished would arrive as a surprise. |

### `AlertOut`

One correlated finding with the evidence behind it.

| Field | Type | Required | Description |
|---|---|---|---|
| `review_state` | string | no |  |
| `alert_id` | integer | yes |  |
| `iteration_id` | integer | yes |  |
| `city` | string | yes | "Name, ST", or just the name if no state. |
| `track` | string | yes | One of the loaded mission's tracks. Not an enum in this schema because the permitted values come from a mission pack read at startup; `GET /v1/capabilities` reports the live set as `mission.tracks`. |
| `confidence` | Confidence | yes | Computed deterministically in Python. Not a probability, and not to be presented to a decision-maker as one. |
| `summary` | string | yes | One or two sentences, written by a language model that is never shown the score and cannot change it. |
| `caveat` | string | no | Deterministic disclosure, written in Python and never by the model, so it cannot be softened or dropped. Present when a source could not be collected — naming the family and, for a partial loss, the endpoint — and ALSO when this iteration contributed no evidence of its own, in which case it names the iteration that last did and how old the contributing signals are. Either can appear without the other. |
| `earliest_eta` | string | no | Soonest arrival among the contributing flights. The most actionable single field when it is present. |
| `created_at` | string | yes |  |
| `evidence_url` | string | yes | Drill-down to every contributing signal and the arithmetic. |
| `social_posts` | object[] | no | Contributing social signals. |
| `flights` | object[] | no | Contributing flights. `category_confidence: AMBIGUOUS` means the source could not determine the category, not that it is unknown to the operator. |
| `lodging` | object[] | no | Short-term-rental availability, and hotel price where the price signal is enabled. |
| `rental_cars` | object[] | no | Rental-car availability by class. |

### `AlertTupleOut`

`?format=tuple`: the four evidence groups as a positional array.

The requirement asks for an array of tuples of (social posts, flights,
lodging changes, car availability), each carrying a summary and a
confidence score. JSON has no tuple type, so `evidence` is a fixed
four-element array in that exact order and the summary and confidence stay
addressable beside it rather than being appended as positions five and six.

| Field | Type | Required | Description |
|---|---|---|---|
| `alert_id` | integer | yes |  |
| `city` | string | yes |  |
| `track` | string | yes | One of the loaded mission's tracks. Not an enum in this schema because the permitted values come from a mission pack read at startup; `GET /v1/capabilities` reports the live set as `mission.tracks`. |
| `summary` | string | yes |  |
| `confidence` | Confidence | yes |  |
| `caveat` | string | no |  |
| `evidence` | object[][] | yes |  |
| `evidence_url` | string | yes |  |

### `CancelIn`

Why a run is being stopped. The reason is recorded, not just the act.

| Field | Type | Required | Description |
|---|---|---|---|
| `requested_by` | string | no |  |
| `reason` | string | no |  |

### `CancelOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `iteration_id` | integer | yes |  |
| `status` | string | yes |  |
| `cancel_requested_at` | string | yes |  |
| `will_still_run` | string[] | no |  |
| `note` | string | no |  |

### `CapabilitiesOut`

What the system can and cannot do, before anyone asks it to.

Exists so that "no alerts for this county" can be distinguished from "this
county was never collectable". Without it the two are identical on the
wire, and the second one silently reads as reassurance.

| Field | Type | Required | Description |
|---|---|---|---|
| `mission` | object | no | Which mission definition this deployment loaded, and the vocabularies it supplies. **This is where the permitted `track` and `location_type` values come from.** They are reported here rather than declared as enums in this schema because they are read from a mission pack at startup, so no fixed enum could describe them — a deployment running a different pack accepts different values against this same contract. Carries `id`, `version`, `digest`, `tracks` and `location_types`; `digest` is the hash stamped on every receipt, so a client holding an alert can tell whether the definition has changed since it was written. When `configured` is false no mission is loaded: the database and this contract are available but no iteration can run, and `effect` says so. |
| `tracks` | string[] | no | Tracks defined by the loaded mission. Not an enum in this schema because the permitted values come from a mission pack read at startup; `GET /v1/capabilities` reports the live set as `mission.tracks`. |
| `signal_families` | string[] | no |  |
| `providers` | object | no |  |
| `geography` | object | no |  |
| `retention` | object | no |  |
| `cities` | CapabilityCity[] | no |  |
| `open_rights_questions` | object[] | no |  |
| `caveats` | string[] | no |  |

### `CapabilityCity`

What this deployment could actually collect for one jurisdiction.

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | string | yes |  |
| `state` | string | no |  |
| `resolved` | boolean | yes |  |
| `resolved_by` | string | yes |  |
| `airports` | string[] | no |  |
| `pickup_location` | string | no |  |
| `supported_sources` | string[] | no |  |
| `unsupported_sources` | string[] | no |  |
| `limitations` | string[] | no |  |

### `CitiesIn`

| Field | Type | Required | Description |
|---|---|---|---|
| `cities` | CityIn[] | yes |  |

### `CityIn`

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | string | yes |  |
| `state` | string | no |  |
| `key_locations` | KeyLocationIn[] | no |  |

### `CityOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `city_id` | integer | yes |  |
| `name` | string | yes |  |
| `state` | string | no |  |
| `is_seed` | boolean | no |  |
| `admitted_by` | string | no |  |
| `key_locations` | string[] | no |  |
| `airports` | string[] | no |  |
| `pickup_location` | string | no |  |

### `Confidence`

An ALERT's score and band. NONE is impossible here by construction.

| Field | Type | Required | Description |
|---|---|---|---|
| `score` | number | yes | 0..1. A weighted sum of per-family weight x quality, not a probability. |
| `band` | `LOW` | `MEDIUM` | `HIGH` | yes | Cannot be HIGH when any contributing source failed. |

### `CorrelationConfidence`

A CORRELATION's score and band, which may be NONE.

Separate from `Confidence` rather than widening it. NONE is a real computed
outcome — "we looked and this did not reach a band" — and is stored so that
finding nothing is on the record; but an alert is never written for one, so
the alert contract keeps the narrower type that says so.

Reusing the alert model here was a 500 on the very case the correlation
routes exist to expose: a band-NONE correlation failed validation and the
listing crashed. The tests missed it because their fixture raises
`alert_min_score`, which produces a BELOW_FLOOR row with a real band —
never the sub-threshold band itself.

| Field | Type | Required | Description |
|---|---|---|---|
| `score` | number | yes |  |
| `band` | `NONE` | `LOW` | `MEDIUM` | `HIGH` | yes | NONE means no band rule fired at all, which is a result rather than an absence of one. |

### `CorrelationOut`

One scored city/track, whether or not it became an alert (8.7b).

| Field | Type | Required | Description |
|---|---|---|---|
| `correlation_id` | integer | yes |  |
| `city` | string | yes |  |
| `track` | string | yes | One of the loaded mission's tracks. Not an enum in this schema because the permitted values come from a mission pack read at startup; `GET /v1/capabilities` reports the live set as `mission.tracks`. |
| `confidence` | CorrelationConfidence | yes |  |
| `distinct_types` | integer | no |  |
| `data_completeness` | number | no | Share of the four signal families collected at all. A family counts as missing only when NOTHING in it was collected — see `failed_families`. |
| `band_capped` | boolean | no |  |
| `failed_sources` | string[] | no | Every failed collection as `SOURCE_TYPE:endpoint`, e.g. `LODGING:/search`. Finer than `failed_families`, and does NOT drive `data_completeness`. |
| `failed_families` | string[] | no | Signal families with NOTHING collected — what `data_completeness` counts. A family in `failed_sources` but absent here was degraded, not lost. |
| `rule_trace` | string | no |  |
| `contributions` | object | no |  |
| `alert_decision` | `ALERTED` | `BELOW_FLOOR` | `BAND_NONE` | no | What ALERTING concluded. BELOW_FLOOR: under `correlation.alert_min_score`. BAND_NONE: no band qualified. Null means ALERTING has not run for this iteration yet, which is not the same as deciding against. |
| `alert_decision_reason` | string | no | The decision in words, with the numbers that produced it. Written by the agent that decided, so a reader does not have to hold the configuration to reconstruct it. |
| `alerted` | boolean | no |  |
| `computed_at` | string | no |  |
| `evidence_url` | string | no |  |

### `CorrelationsOut`

Everything an iteration scored. The sub-threshold rows are the point.

A correlation that produced no alert is the near miss, and the near misses
are what the interim floors in `correlation` and `sensitivity` are meant to
be calibrated from. They have no alert row, and every other route into the
evidence surface resolves `alerts.correlation_id` first.

| Field | Type | Required | Description |
|---|---|---|---|
| `iteration_id` | integer | yes |  |
| `counts` | object | no |  |
| `correlations` | CorrelationOut[] | no |  |

### `DiscardIn`

| Field | Type | Required | Description |
|---|---|---|---|
| `expect` | string | no |  |
| `confirm` | boolean | no |  |

### `DiscardOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `iteration_id` | integer | yes |  |
| `stage` | string | yes |  |
| `deleted` | object | no |  |
| `queries_reset` | integer | no |  |
| `units_spent` | object | no |  |
| `not_reverted` | string[] | no |  |
| `degradations_retracted` | integer | no | Notes this stage had recorded about what it could not do, removed with its rows. A gap the re-run may close must not outlive the evidence for it. |
| `next_stage` | string | yes |  |

### `EpochOut`

One process instance.

| Field | Type | Required | Description |
|---|---|---|---|
| `epoch_id` | integer | yes |  |
| `started_at` | string | yes |  |
| `host` | string | yes |  |
| `pid` | integer | yes |  |
| `entry_point` | string | yes |  |
| `ended_at` | string | no |  |
| `shutdown_kind` | `CLEAN` | `TIMEOUT` | `UNKNOWN` | no | UNKNOWN is written by a LATER process onto one it found open, and leaves ended_at null — nothing knows when a killed process died, and inventing a time that later reads as fact is worse than admitting the gap. |
| `stranded` | integer[] | no |  |

### `ErrorOut`

Every non-2xx response. FastAPI's HTTPException shape, made explicit.

Declared so the generated schema documents the failure modes: a contract
that lists only the success path tells a client nothing about what to
handle, and 401, 409 and 503 are all reachable on routes whose happy path
looks unremarkable.

| Field | Type | Required | Description |
|---|---|---|---|
| `detail` | string | yes |  |

### `EvidenceOut`

Full drill-down: every contributing signal back to its raw payload.

| Field | Type | Required | Description |
|---|---|---|---|
| `alert_id` | integer | no |  |
| `correlation_id` | integer | no |  |
| `city` | string | yes |  |
| `track` | string | yes | One of the loaded mission's tracks. Not an enum in this schema because the permitted values come from a mission pack read at startup; `GET /v1/capabilities` reports the live set as `mission.tracks`. |
| `confidence` | CorrelationConfidence | yes |  |
| `summary` | string | no |  |
| `caveat` | string | no |  |
| `alert_decision` | `ALERTED` | `BELOW_FLOOR` | `BAND_NONE` | no |  |
| `alert_decision_reason` | string | no |  |
| `rule_trace` | string | no |  |
| `contributions` | object | no |  |
| `distinct_types` | integer | no |  |
| `data_completeness` | number | no | Share of the four signal families that were collected at all. A family counts as missing only when NOTHING in it was collected (9.11): if the lodging availability endpoint failed and the price endpoint succeeded, lodging was measured and does not count against this. Compute it as `1 - len(failed_families) / 4`. |
| `failed_sources` | string[] | no | Every failed collection as `SOURCE_TYPE:endpoint` — for example `LODGING:/search`. The endpoint is the query's ENTRY endpoint, so a lodging pairing shortfall that happens at `/availability` still reports `/search`; the query row's `skip_reason` carries the precise cause. Where no endpoint was recorded the bare source type appears. This is finer than `failed_families` and does NOT drive `data_completeness`. |
| `failed_families` | string[] | no | Signal families with NOTHING collected. This is what `data_completeness` counts and what the caveat names; a family present in `failed_sources` but absent here was degraded rather than lost. |
| `band_capped` | boolean | no |  |
| `alternatives` | object[] | no | Competing explanations for this evidence, each with a stable `code`, a `statement`, and `weakened_by` — what in THIS correlation argues against it, empty when nothing does, because an unanswered alternative serves a reader better than a manufactured rebuttal. Deterministic, never model-written: the loaded mission supplies the explanations per signal family and the engine decides which of them this evidence admits, so a booking-only correlation admits ordinary-demand explanations that a multi-family one does not. `null` on a correlation computed before these rules existed; an empty list means no family contributed and there is nothing to explain away. |
| `evidence_freshness` | object | no |  |
| `flight_baseline` | object | no |  |
| `collection` | object | no | Contributing signals counted by how directly they are known, so acquisition is one comparable value rather than four per-family provenance fields. `DIRECT` (retrieved from the party that holds the record), `INTERMEDIARY_LIVE` (a vendor's live call against a platform or publisher), `INTERMEDIARY_CACHED` (served from a vendor cache and billed as though fresh, so the ledger is no guide to freshness), `UNRECORDED` (the default, so a writer that forgets produces a visible absence rather than a plausible claim). Empty classes are omitted. The absence of `DIRECT` is the standing fact about this deployment: every provider here is an intermediary. |
| `signals` | object[] | no |  |
| `receipt` | object | no | Provenance for the model call behind `summary`, or null when the deterministic fallback wrote it. A loose object because the field set follows the provider echo, but three keys are the point of it: `mission_hash` — compare against `mission.digest` from `GET /v1/capabilities` to tell whether the definition has changed since this judgement was made; `config_hash` — compare against `SessionOut.config_hash` for the same question about settings; and `prompt_hash`, which says two judgements were made under the same criteria without disclosing what they are. |

### `HTTPValidationError`

| Field | Type | Required | Description |
|---|---|---|---|
| `detail` | ValidationError[] | no |  |

### `HealthOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `status` | `ok` | `degraded` | yes |  |
| `database` | string | yes |  |
| `schema_version` | integer | yes |  |
| `dry_run` | boolean | no |  |
| `debug_endpoints` | boolean | no |  |
| `active_sessions` | integer | no |  |
| `running_iterations` | integer[] | no |  |
| `epoch_id` | integer | no |  |
| `interrupted_iterations` | integer[] | no |  |
| `budget` | object | no |  |
| `connectors` | object | no |  |
| `retention` | object | no |  |

### `InterruptedOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `iteration_id` | integer | yes |  |
| `session_id` | integer | yes |  |
| `seq` | integer | yes |  |
| `kind` | `INTERRUPTED` | `OPEN` | no | INTERRUPTED: a process died mid-run and the reconcile stamped it. OPEN: never finished by any route — a manual walk left partway, or a cancellation recorded against an iteration that was not on a worker. Both block a new iteration and both are closed the same two ways; the distinction tells you what happened, not what to do. |
| `interrupted_at` | string | no | Null for kind=OPEN. Only the crash reconcile stamps it. |
| `interrupted_stage` | string | no |  |
| `stage_pointer` | string | no |  |
| `started_at` | string | no |  |
| `resume_url` | string | no |  |
| `abandon_url` | string | no |  |
| `plan_url` | string | no |  |

### `IterationAccepted`

| Field | Type | Required | Description |
|---|---|---|---|
| `iteration_id` | integer | yes |  |
| `session_id` | integer | yes |  |
| `seq` | integer | no |  |
| `status` | `RUNNING` | `PENDING` | `FINISHED` | yes |  |
| `stage` | string | yes |  |
| `poll_url` | string | yes |  |
| `next_stage` | string | no |  |
| `retry_of_iteration_id` | integer | no |  |
| `budget_plan` | object | no |  |

### `IterationIn`

How to run the iteration being created.

`manual` creates it and stops, for stepping through the stages one at a
time. `auto` is the normal path and the default: agents must not solicit
user interaction inside an iteration, so nothing between stage 1 and stage 8
can ask for anything.

| Field | Type | Required | Description |
|---|---|---|---|
| `mode` | `auto` | `manual` | no |  |

### `IterationOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `iteration_id` | integer | yes |  |
| `session_id` | integer | yes |  |
| `seq` | integer | yes |  |
| `stage` | string | yes | One of the eight pipeline stages, or COMPLETE / FAILED. |
| `outcome` | `COMPLETE` | `PARTIAL` | `FAILED` | no | Null while running. PARTIAL means the sequence finished but something was lost — the alerts are real findings made from incomplete evidence. FAILED means a prerequisite stage never ran and the alerts should not be read at all. |
| `running` | boolean | no |  |
| `anchor_at` | string | no | The instant the correlation window centres on. |
| `started_at` | string | no |  |
| `finished_at` | string | no |  |
| `next_stage` | string | no | What a debug step would run next, if any. |
| `counts` | object | no | Queries enqueued/executed/failed/skipped, and rows written per analytical table. `triage_decisions` here is a TOTAL; see `triage_states` for what those decisions were. |
| `triage_states` | object | no | Judgements by durable outcome: `ACCEPTED`, `REJECTED`, `UNDECIDED`, `INVALID_OUTPUT`, `MODEL_ERROR`. Absent keys are zero. The distinction is the point and it is not recoverable from the total: REJECTED is a judgement that the item is not relevant, while UNDECIDED, INVALID_OUTPUT and MODEL_ERROR are posts that were collected and paid for and never judged — a coverage gap, which caps the confidence band and reaches the alert caveat. Before this field a client could only infer that from free-text `degradations`. |
| `budget` | object | no | Units billed this iteration, per provider. Always all four providers, zero included. |
| `status` | `PENDING` | `RUNNING` | `INTERRUPTED` | `FINISHED` | no | The lifecycle state, which `outcome` alone cannot give you: an interrupted iteration has a null outcome and is not running, and a client watching those two fields would never stop. Stop on FINISHED or INTERRUPTED. |
| `interrupted_at` | string | no | Set when a process ended without finishing this run. It never clears, so the history survives a later resume. |
| `interrupted_stage` | string | no | The stage that was in flight when it died. |
| `retry_of_iteration_id` | integer | no | Set when this iteration is a re-triage of an earlier one (8.8). It re-judged that iteration's unanswered posts, inherited its `anchor_at`, and did not re-collect. The parent is unmodified — both records stand as what each run did. |
| `resumable` | boolean | no | True while the iteration awaits a resume or an abandon — for a crash-interrupted run and equally for one merely left open. Either blocks a new iteration on this session, so either needs closing. |
| `degradations` | string[] | no |  |
| `error_message` | string | no |  |

### `KeyLocationIn`

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | string | yes |  |
| `address` | string | no |  |
| `lat` | number | no |  |
| `lon` | number | no |  |
| `location_type` | string | no | One of the loaded mission's `location_types`, which `GET /v1/capabilities` reports. Not an enum in this schema because the permitted values come from a mission pack read at startup; an unknown one is refused with a 422 naming the value and listing what was allowed. |

### `QueueOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `session_id` | integer | yes |  |
| `iteration_id` | integer | no |  |
| `status_counts` | object | no |  |
| `decision_counts` | object | no |  |
| `scheduled_ahead` | integer | no |  |
| `queries` | object[] | no |  |
| `decisions` | object[] | no |  |

### `RecoveryOut`

What the current process found when it started, and what still blocks.

| Field | Type | Required | Description |
|---|---|---|---|
| `epoch` | EpochOut | yes |  |
| `previous_epoch` | EpochOut | no |  |
| `interrupted` | InterruptedOut[] | no |  |
| `blocking` | InterruptedOut[] | no |  |
| `refused_epochs` | integer[] | no |  |

### `RecoveryPlanOut`

What resuming would do, before it does any of it.

| Field | Type | Required | Description |
|---|---|---|---|
| `iteration_id` | integer | yes |  |
| `session_id` | integer | yes |  |
| `resume_from` | string | yes |  |
| `derived_by` | `RECONCILED` | `IN_FLIGHT` | `BETWEEN_STAGES` | `NOTHING_RAN` | yes | How the resume point was decided. RECONCILED is the durable one, read from the row the reconcile stamped. |
| `stage_pointer` | string | yes |  |
| `interrupted_stage` | string | no |  |
| `paid` | boolean | yes | True when resuming would collect again at a vendor. `confirm_respend` is then required. |
| `queries_to_recollect` | object[] | no |  |
| `already_banked` | object[] | no | Paid for and stored before the crash. Not re-bought, and not counted as coverage either — a completed query with no signal reads as 'we looked and found nothing'. |
| `already_spent` | object | no |  |
| `estimated_units_upper_bound` | object | no | An upper bound, NOT a price. FR24 bills per record returned, so no pre-flight figure can be exact — judge from queries_to_recollect. |

### `ResumeIn`

How to restart an interrupted iteration.

Two fields and no third override. An *earlier* `from_stage` needs no extra
ceremony — naming a stage is itself the explicit act — while a *later* one
is refused outright, because skipping a stage that never ran is never what
anyone wants. Collapsing two acknowledgements into one boolean is how
confirmations stop meaning anything.

| Field | Type | Required | Description |
|---|---|---|---|
| `from_stage` | string | no | Override the derived resume point. Must not be later than it. Use discard-last-stage to go further back. |
| `confirm_respend` | boolean | no | Required when the plan would collect again at a vendor. Nothing already spent is reclaimed. |

### `RetryTriageIn`

How to re-judge the posts a model failure lost (8.8).

| Field | Type | Required | Description |
|---|---|---|---|
| `batch_size` | integer | no | Posts per model call for the retry. Omit to use `triage.batch_size` — the retry halves it on each TruncatedResponse down to one anyway, so an override is only needed to start smaller than the configured value. |

### `ReviewIn`

A human decision about DISTRIBUTION. Nothing analytical moves.

| Field | Type | Required | Description |
|---|---|---|---|
| `review_state` | `UNREVIEWED` | `RELEASED` | `WITHHELD` | yes |  |
| `reviewed_by` | string | no |  |
| `note` | string | no |  |

### `ReviewOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `alert_id` | integer | yes |  |
| `review_state` | string | yes |  |
| `reviewed_at` | string | no |  |
| `reviewed_by` | string | no |  |
| `note` | string | no |  |

### `SessionIn`

Initialisation. The key locations are what lodging queries anchor on.

Give either `cities` inline or `input_set` — exactly one. The response
echoes the resolved geography either way, so an operator can see what they
created rather than trust that a file still says what they remember.

| Field | Type | Required | Description |
|---|---|---|---|
| `label` | string | no |  |
| `cities` | CityIn[] | no | Inline jurisdictions. Omit when using `input_set`. |
| `input_set` | string | no | NAME of a file in the configured input directory (`inputs.dir`), without the extension — for example 'example'. Not a path: this field would otherwise be a file-disclosure primitive, and an authenticated caller is not thereby trusted with the filesystem. An unresolvable city in the file is refused by name, never silently dropped. |
| `expand_cities` | boolean | no |  |
| `tracks` | string[] | no | Which of the loaded mission's tracks to score. Defaults to all of them. `GET /v1/capabilities` reports the permitted values, which are not an enum here because they come from a mission pack read at startup. |
| `tunables` | object | no | Per-session configuration overrides, nested as in config.yaml. Settable sections: triage, sensitivity, correlation, windows, tipping, budget. Anything else is a 422 naming the field — a setting that was accepted and ignored is worse than one that was refused. |

### `SessionOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `session_id` | integer | yes |  |
| `label` | string | no |  |
| `status` | string | no |  |
| `created_at` | string | no |  |
| `expand_cities` | boolean | no |  |
| `tracks` | string[] | no | Tracks defined by the loaded mission. Not an enum in this schema because the permitted values come from a mission pack read at startup; `GET /v1/capabilities` reports the live set as `mission.tracks`. |
| `cities` | CityOut[] | no |  |
| `warnings` | string[] | no |  |
| `tunables` | object | no | Per-session configuration overrides in force, normalised. Empty means every stage runs on the server's own configuration. |
| `config_hash` | string | no | Fingerprint of the effective analytical configuration. Matches `config_hash` on every receipt this session's iterations produce. |

### `StageOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `stage` | string | yes |  |
| `status` | string | yes |  |
| `started_at` | string | no |  |
| `finished_at` | string | no |  |
| `error_message` | string | no |  |
| `wrote` | object | no |  |
| `decisions` | object | no |  |
| `agents` | object[] | no |  |
| `api_calls` | object | no |  |
| `log` | object[] | no |  |
| `correlations` | object[] | no | What CORRELATING concluded. Each entry carries `evidence_url` — the drill-down works for a correlation with no alert, which is the case that had no route at all before 8.7(b). |
| `skips` | object | no | Why collected posts did not reach the model (8.9). STALE is a decision — older than `triage.max_post_age_hours`. The PAYLOAD_* and ITEM_* reasons are defects: evidence that was collected and paid for and then could not be read, and a PAYLOAD_* reason costs every post in that response. Empty for every other stage. |

### `StagesOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `iteration_id` | integer | yes |  |
| `stage` | string | yes |  |
| `outcome` | string | no |  |
| `next_stage` | string | no |  |
| `stages` | StageOut[] | no |  |

### `StepIn`

| Field | Type | Required | Description |
|---|---|---|---|
| `expect` | string | no |  |

### `StepOut`

| Field | Type | Required | Description |
|---|---|---|---|
| `iteration_id` | integer | yes |  |
| `stage` | string | yes |  |
| `ok` | boolean | yes |  |
| `next_stage` | string | no |  |
| `outcome` | string | no |  |
| `report` | StageOut | yes |  |

### `ValidationError`

| Field | Type | Required | Description |
|---|---|---|---|
| `loc` | string | integer[] | yes |  |
| `msg` | string | yes |  |
| `type` | string | yes |  |
| `input` | object | no |  |
| `ctx` | object | no |  |

---

## Captured examples

Real exchanges, recorded by driving the application through a full iteration with fixture-backed connectors and a frozen clock. Two things to read past:

* The alert summaries come from a stub model. They are placeholders for the shape, not a sample of the system's prose.
* Every `units_spent` and `budget` figure is **zero**, because `dry_run` records zero units by design. On a real run these carry the credits actually billed.

| # | Exchange | Status | Note |
|---|---|---|---|
| [`01-create-session`](examples/01-create-session.json) | `POST /v1/sessions` | `201` | Airports and pickup points resolve here, so a mapping gap is a warning at init rather than a skipped query hours later. |
| [`01b-session-from-input-set`](examples/01b-session-from-input-set.json) | `POST /v1/sessions` | `201` | The geography from `inputs/<name>.yaml` instead of inline. `input_set` is a NAME resolved inside `inputs.dir`, never a path. All-or-nothing: a city the geo table cannot place is refused by name with a 422, because a session quietly missing a jurisdiction would report a true absence of evidence about a place nobody looked at. The response echoes what actually resolved. |
| [`01c-input-set-path-refused`](examples/01c-input-set-path-refused.json) | `POST /v1/sessions` | `422` | Authenticated is not the same as trusted with the filesystem. A path here would be a file-disclosure primitive whatever the intent behind it. |
| [`01d-session-with-tunables`](examples/01d-session-with-tunables.json) | `POST /v1/sessions` | `201` | Per-session overrides, nested as in config.yaml. The response echoes them and returns `config_hash` — the same value every receipt this session produces carries, so a client can confirm after the fact that a judgement was made under the settings it asked for. |
| [`01e-tunable-refused`](examples/01e-tunable-refused.json) | `POST /v1/sessions` | `422` | Refused by name. Credentials, provider endpoints, retention ceilings and deployment controls are server-owned; so is an unknown field. A tunable that was accepted and then ignored is the defect this replaced — the client would have been told its request succeeded while collection ran on other settings. |
| [`01f-tunable-cap-cannot-be-raised`](examples/01f-tunable-cap-cannot-be-raised.json) | `POST /v1/sessions` | `422` | A session may lower a spending cap and never raise one. The budget being protected is the operator's. |
| [`02-unauthenticated`](examples/02-unauthenticated.json) | `GET /v1/sessions/1` | `401` | Every route except /v1/healthz needs the bearer token. |
| [`03-read-session`](examples/03-read-session.json) | `GET /v1/sessions/1` | `200` |  |
| [`04-trigger-iteration`](examples/04-trigger-iteration.json) | `POST /v1/sessions/1/iterations?wait=true` | `200` | Without ?wait=true this returns 202 immediately and the run continues on a worker; poll_url is the same either way. |
| [`05-poll-iteration`](examples/05-poll-iteration.json) | `GET /v1/iterations/1` | `200` | The poll target. `degradations` is what the run could not do; an empty list and a missing field differ. |
| [`06-alerts`](examples/06-alerts.json) | `GET /v1/sessions/1/alerts` | `200` | Most severe first, then most recent. |
| [`07-alerts-tuple`](examples/07-alerts-tuple.json) | `GET /v1/sessions/1/alerts?format=tuple` | `200` | `evidence` is a fixed four-element array: social posts, flights, lodging, rental cars. |
| [`08-evidence`](examples/08-evidence.json) | `GET /v1/alerts/1/evidence` | `200` | Every contributing signal back to its raw payload and the queue row that fetched it, with the arithmetic that produced the score. Assembled from the CORRELATION — this route resolves it and delegates — so 40 returns the identical shape for a correlation that produced no alert. |
| [`08b-correlations`](examples/08b-correlations.json) | `GET /v1/iterations/1/correlations` | `200` | Everything the iteration scored, alerting or not. `alert_decision` says what ALERTING concluded about each and why, recorded by the agent that decided rather than left for a reader to infer from `score` and a config value. |
| [`09-queue`](examples/09-queue.json) | `GET /v1/sessions/1/queue?iteration_id=1&limit=5` | `200` | Refusals are the point: a query that was deduped, cooled down, capped or priced out is a decision. |
| [`10-create-manual-iteration`](examples/10-create-manual-iteration.json) | `POST /v1/sessions/1/iterations` | `202` | mode=manual creates the iteration without running it, for stepping. Note the four-hour gap from the first iteration: the cooldown guard is keyed on the query hash across all iterations. |
| [`11-step`](examples/11-step.json) | `POST /v1/iterations/2/step` | `200` | One stage, with the report of what it did. `expect` is a guard, not a selector. |
| [`12-step-wrong-stage`](examples/12-step-wrong-stage.json) | `POST /v1/iterations/2/step` | `409` | The guard refusing: a client that lost track of the pointer must not run the wrong stage and spend money doing it. |
| [`13-stages`](examples/13-stages.json) | `GET /v1/iterations/2/stages` | `200` | All eight, including SCHEDULING which has not run. |
| [`14-stage`](examples/14-stage.json) | `GET /v1/iterations/2/stages/TRIAGING` | `200` | One stage in detail: what it wrote, which agents ran, what it spent, and its log. |
| [`15-discard-last-stage`](examples/15-discard-last-stage.json) | `POST /v1/iterations/2/discard-last-stage` | `200` | Discards ALERTING. Only the last stage, one at a time; repeated calls walk backwards. |
| [`16-discard-needs-confirm`](examples/16-discard-needs-confirm.json) | `POST /v1/iterations/2/discard-last-stage` | `409` | Refused: discarding a collection stage means buying the same data again at the vendor, so it takes an explicit acknowledgement and reports what was already spent. |
| [`17-discard-confirmed`](examples/17-discard-confirmed.json) | `POST /v1/iterations/2/discard-last-stage` | `200` | The same call, acknowledged. `queries_reset` are back to PENDING and will be collected again; `units_spent` is not reclaimed. |
| [`18-add-cities`](examples/18-add-cities.json) | `POST /v1/sessions/1/cities` | `201` | Between iterations, never during one. |
| [`19-healthz`](examples/19-healthz.json) | `GET /v1/healthz` | `200` | Unauthenticated liveness. ?deep=true probes the four vendors and does require the token. |
| [`20-not-found`](examples/20-not-found.json) | `GET /v1/sessions/999` | `404` | The application's error shape. |
| [`21-validation-error`](examples/21-validation-error.json) | `POST /v1/sessions` | `422` | Pydantic's own 422 shape, which is richer than the application's {"detail": "..."} and distinct from it. |
| [`22-recovery`](examples/22-recovery.json) | `GET /v1/recovery` | `200` | What this process found when it started. Each entry carries the two URLs that can close it; there is no third option and no force-unstick. |
| [`23-interrupted-iteration`](examples/23-interrupted-iteration.json) | `GET /v1/iterations/3` | `200` | status INTERRUPTED is what lets a watching client stop. Before it, an interrupted run returned running:false, outcome:null forever. |
| [`24-recovery-plan`](examples/24-recovery-plan.json) | `GET /v1/iterations/3/recovery-plan` | `200` | Read-only. `estimated_units_upper_bound` is a bound, not a price — FR24 bills per record returned, so judge from queries_to_recollect. |
| [`25-resume-needs-confirm`](examples/25-resume-needs-confirm.json) | `POST /v1/iterations/3/resume` | `409` | Refused: resuming would collect again at the vendors and nothing already spent is reclaimed. |
| [`26-trigger-blocked`](examples/26-trigger-blocked.json) | `POST /v1/sessions/4/iterations` | `409` | 409 while an interrupted iteration is outstanding. The cooldown is keyed across ALL iterations, so a new run would be silently under-collected. The message names which iteration blocks, which KIND of open it is, and the two URLs that close it — see 37 for the other kind. |
| [`27-abandon`](examples/27-abandon.json) | `POST /v1/iterations/3/abandon` | `200` | The lost collection becomes a counted coverage gap, and the iteration is still scored and alerted from what WAS collected — closing it unscored would be a real cluster reading as silence. |
| [`28-capabilities`](examples/28-capabilities.json) | `GET /v1/capabilities?city=Phoenix%2C+AZ&city=Nowheresville%2C+ZZ` | `200` | What the deployment can and cannot collect, BEFORE a session exists. Nowheresville reports as unsupported rather than quietly returning nothing, which is the distinction the endpoint exists to make. |
| [`30-idempotent-trigger`](examples/30-idempotent-trigger.json) | `POST /v1/sessions/5/iterations` | `202` | This endpoint spends money, so a lost response must be safe to retry. The key opts in. |
| [`31-idempotent-replay`](examples/31-idempotent-replay.json) | `POST /v1/sessions/5/iterations` | `202` | The same key returns the FIRST response and starts nothing. Identical body, plus Idempotent-Replay: true so a caller can tell the difference. |
| [`32-idempotency-key-reused`](examples/32-idempotency-key-reused.json) | `POST /v1/sessions/5/iterations` | `422` | Same key, different body: refused. Replaying the old response would let the caller believe it started a run with its new parameters. |
| [`33-busy-retry-after`](examples/33-busy-retry-after.json) | `POST /v1/sessions/5/iterations` | `409` | An iteration runs for minutes, so a bare 409 leaves a client to guess between giving up and hammering. Retry-After answers it. |
| [`34-cancel`](examples/34-cancel.json) | `POST /v1/iterations/4/cancel` | `200` | Cooperative, not a kill. Honoured at the next stage boundary; CORRELATING and ALERTING still run, because stopping dead would spend the collection budget and then discard the evidence it bought. |
| [`35-review-alert`](examples/35-review-alert.json) | `POST /v1/alerts/1/review` | `200` | A human gate on DISTRIBUTION only. The score, band and evidence are untouched — an alert withheld for being unhelpful must stay in the record at its computed confidence. |
| [`36-alerts-released-only`](examples/36-alerts-released-only.json) | `GET /v1/sessions/1/alerts?review_state=RELEASED` | `200` | What a client distributing onward should ask for. The unfiltered listing deliberately returns every alert, because an operator cannot review what the API hides. |
| [`37-trigger-blocked-open`](examples/37-trigger-blocked-open.json) | `POST /v1/sessions/5/iterations` | `409` | 409 for an iteration that is merely OPEN rather than crash-interrupted. Same status, same two remedies, and no Retry-After for the same reason as 26: waiting never clears it. The kind is what tells an operator what happened; it does not change what to do about it. |
| [`38-recovery-blocking`](examples/38-recovery-blocking.json) | `GET /v1/recovery` | `200` | `interrupted` is what THIS process found dead at startup. `blocking` is everything a new iteration would be refused for — a superset, and the list to act on. An iteration left open without a crash appears only here. |
| [`40-retry-triage`](examples/40-retry-triage.json) | `POST /v1/iterations/6/retry-triage?wait=true` | `200` | A batch that overran `llm.max_tokens` recorded every post in it as MODEL_ERROR: the evidence was collected and paid for, only the judgement is missing. This re-judges exactly those posts in a NEW iteration that carries `retry_of_iteration_id`, inherits the parent's `anchor_at` so the correlation window does not slide off the evidence it exists to complete, and does not re-collect. The parent is never edited — both records stand as what each run did. |
| [`39-correlation-evidence`](examples/39-correlation-evidence.json) | `GET /v1/correlations/6/evidence` | `200` | The same drill-down for a correlation that became no alert — here because the deployment's `correlation.alert_min_score` was raised to 0.99. `alert_id`, `summary`, `caveat` and `receipt` are null because no alert and no model call exist; the signals, the arithmetic and the provenance are all present. `alert_decision_reason` states the comparison that produced the outcome, so a reader does not need the configuration to understand it. These near misses are what the interim floors are meant to be calibrated from, and before 8.7(b) they were reachable only by opening the database. |

