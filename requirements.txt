# Surge I&W runtime dependencies.
#
# No agent framework, no ORM, no vector store, no task queue. The scheduler is a
# SQLite table and the trigger is an HTTP call.
#
# Python 3.10+ — the development host is 3.10.13, so avoid 3.11/3.12-only
# constructs: use timezone.utc rather than datetime.UTC, and no PEP 695 aliases.

# LLM access. The OpenAI SDK is used for every provider so that production can
# point at a self-hosted open-weight model by changing base_url alone.
openai>=1.30.0

# Connector HTTP. httpx rather than requests so connectors are respx-testable
# and can go async if collection latency demands it. (Phase 2)
httpx>=0.27.0

# Configuration
pyyaml>=6.0

# REST layer (Phase 6)
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0

# sqlite3 is stdlib — no entry needed.
# Test-only dependencies live in requirements-dev.txt.
